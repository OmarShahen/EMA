
/*const form = document.querySelector("#top-nav-form");
form.addEventListener("submit", (event)=>{
    event.preventDefault();

    const visitorData = {
        name: form["userName"].value,
        email: form["userEmail"].value,
        phone: form["userPhone"].value,
        message: form["userMessage"].value
    };

    const xhttp = new XMLHttpRequest();
    xhttp.open("POST", "http://localhost:5000/ema-mail-server/us-central1/app/ema-mail");
    xhttp.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
    xhttp.send(visitorData);


});*/

